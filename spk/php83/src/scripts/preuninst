#!/usr/bin/env bash
set -euo pipefail

# Stop service if running
if [ -f /var/packages/php83/var/run/php-fpm.pid ]; then
    synoservicectl --stop pkgctl-php83 || true
fi

logger -t php83 "Pre-uninstallation: service stopped"

exit 0
