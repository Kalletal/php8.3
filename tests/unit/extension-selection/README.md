# Unit Harness – Extension Selection Scripts

Use this directory for shell-based tests that mock spksrc runtime expectations (e.g., dependency validation, JSON parsing). Tests should run on the build host before pushing SPKs to the NAS.
